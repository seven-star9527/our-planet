# 测试与优化报告

> 项目：我们的专属星球 | 日期：2026-05-17 | 版本：v1.2

---

## 一、RAG 检索问题分析与优化（已实施）

### 1.1 问题诊断

小七同学在面对"我们第一次约会在哪？""她说过喜欢吃什么？"等问题时无法检索到对应聊天记录，根因分析如下：

| 问题 | 严重度 | 说明 |
|------|--------|------|
| **中文关键词提取失效** | 🔴 严重 | 原 `extractKeywords` 按空格/标点分割中文文本，但中文无空格分隔，导致几乎无法提取有效关键词。例如"我们第一次约会在哪"被当作一个整体，无法匹配数据库中的聊天片段 |
| **停用词过于激进** | 🔴 严重 | "说过""记得""知道""告诉""一起""去""来""到"等词被错误列入停用词。这些恰是记忆检索的核心动词 |
| **查询扩展质量差** | 🟡 中等 | 扩展后的关键词未保留原始问题语义，且中文 prompt 中使用了全角引号导致 TypeScript 编译错误 |
| **混合检索合并策略简陋** | 🟡 中等 | 向量结果和关键词结果仅简单去重，关键词结果统一给 0.4 相似度，无法发挥混合检索互补优势 |
| **无对话历史** | 🟡 中等 | 每次请求独立，追问时丢失上下文，多轮对话体验差 |
| **嵌入文本无截断** | 🟡 中等 | `generateEmbedding` 未限制输入长度，可能与模型 token 上限冲突导致静默失败 |
| **多跳推理范围偏窄** | 🟢 轻微 | 仅对前 3 条高相关记忆的 30 分钟窗口做扩展 |

### 1.2 已实施的优化

#### 1.2.1 中文关键词提取重写 (`src/app/api/chat/route.ts`)

```
优化前：按空格分割 → 中文查询几乎无输出
优化后：字符 bigram/trigram 滑动窗口 + 标点分割双策略
       优先提取 4 字→2 字组合，按长度降序排列，取前 8 个
```

示例效果对比：
- 输入"我们第一次约会在哪里"
- 优化前：`[]`（空，所有词被停用或无法分割）
- 优化后：`["第一次约会", "一次约会", "在哪里", "第一次", "约会", "哪里", "一次", "在哪"]`

#### 1.2.2 停用词表精简

移除了以下关键检索词：`说过`、`记得`、`知道`、`告诉`、`问`、`去`、`来`、`到`、`一起`、`请`、`帮`、`给`

保留了语法虚词：`的`、`了`、`吗`、`呢` 等，并补充了 `把`、`被`、`让`、`应该`、`可能` 等

#### 1.2.3 RRF（倒数排列融合）替代简单去重

```
优化前：seenIds Set 去重，关键词结果统一给 0.4 分
优化后：RRF 算法融合两个排序列表，K=60
       同时出现在两个列表中 → 分数更高
       仅在一个列表中 → 按排名计算分数
```

#### 1.2.4 对话历史支持

- API 端：接受 `sessionId` 和 `history` 参数，内存 Map 存储（最多 10 条）
- 前端：生成唯一会话 ID，每次请求携带最近 10 条对话历史
- 系统提示中注入近期对话上下文

#### 1.2.5 其他优化

- 嵌入输入截断至 8000 字符，防止超限
- 查询扩展保留原始问题，避免语义丢失
- 多跳推理扩展至前 5 条 high 记忆、60 分钟窗口
- 回退排序阈值保持 0.15，避免过于严格

### 1.3 建议后续优化（未实施）

| 建议 | 优先级 | 说明 |
|------|--------|------|
| 创建 pgvector IVFFlat 索引 | 高 | `CREATE INDEX ON chat_messages USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);` 可大幅提升向量搜索性能 |
| 升级嵌入模型 | 中 | 智谱 `embedding-2` (1536维) 可升级为 `embedding-3` 或 BGE 系列以获得更好语义理解 |
| 专用重排序模型 | 中 | 当前用 DeepSeek 做重排序（每次额外 API 调用），可替换为 `bge-reranker-v2-m3` 本地部署，降低成本与延迟 |
| 会话持久化 | 中 | 当前会话存储在内存中，服务重启丢失。建议迁移至 Redis 或数据库 |
| 意图分类优化 | 低 | 当前用正则匹配，可替换为 LLM 分类或专用分类模型以提高准确率 |

---

## 二、功能缺陷清单

### 2.1 🔴 高优先级

#### 2.1.1 上传文件无类型校验 (`src/app/api/upload/route.ts`)

```typescript
// 当前：任何文件类型均可上传
const buffer = Buffer.from(await file.arrayBuffer());
// 风险：可上传 HTML/JS/SVG 等文件，存在 XSS 风险
```

**建议**：添加白名单校验，仅允许图片（jpg/png/gif/webp）和视频（mp4/mov）格式。

#### 2.1.2 Docker 硬编码数据库密码 (`docker-compose.yml`)

```yaml
environment:
  POSTGRES_PASSWORD: wzhpxy2026  # 明文硬编码
```

**建议**：使用 Docker secrets 或 `.env` 文件引用 `${DB_PASSWORD}`。

#### 2.1.3 认证机制薄弱 (`src/middleware.ts`)

仅依赖 `user_role` cookie（值为 `boy` 或 `girl`），无任何密码或 token 验证。任何人可通过手动设置 cookie 绕过登录。

**建议**：如果部署在公网，至少添加简单的共享密码验证；如果仅内网使用，当前机制可接受。

### 2.2 🟡 中优先级

#### 2.2.1 点赞无法取消 (`src/actions/moments.ts`)

```typescript
export async function toggleLike(momentId: number, emoji: string) {
  // 函数名为 toggleLike 但只实现了 add 逻辑
  await prisma.like.create({ data: { ... } });
}
```

**建议**：检查是否已点赞，是则删除，否则创建。

#### 2.2.2 生理期日期处理 Bug (`src/actions/period.ts`)

```typescript
const existing = await prisma.periodRecord.findFirst({
  where: {
    startDate: {
      gte: new Date(date.setHours(0, 0, 0, 0)),  // date 被原地修改
      lt: new Date(date.setHours(23, 59, 59, 999)) // 使用了被修改后的 date
    }
  }
});
```

`date.setHours()` 会原地修改 `date` 对象，导致 `gte` 的边界被后续 `lt` 的调用影响。**建议**：使用独立的 `new Date()` 对象。

#### 2.2.3 留言板活跃过滤在 JS 端 (`src/actions/messages.ts`)

```typescript
// 先查全量再 JS 过滤，数据量大时性能差
const messages = await prisma.homeMessage.findMany({ ... });
const activeMessages = messages.filter(msg => { ... });
```

**建议**：使用 Prisma 的 `AND`/`OR` 条件直接在数据库层过滤，或使用 raw SQL。

#### 2.2.4 无数据分页

所有列表查询（手账、留言、足迹等）均使用 `findMany` 无 `skip`/`take` 限制，数据量大时会一次性加载所有记录。

### 2.3 🟢 低优先级

#### 2.3.1 控制台调试日志

生产代码中大量 `console.log` / `console.warn`，建议使用结构化日志或环境变量控制。

#### 2.3.2 TypeScript any 类型

多处使用 `any` 类型（如 `prisma: any`、`rawVectorResults: any[]`），降低了类型安全性。

#### 2.3.3 无自动化测试

项目无任何单元测试、集成测试或 E2E 测试。

#### 2.3.4 缺少 Loading/Error 状态

部分页面缺少加载骨架屏和错误边界组件。

---

## 三、性能优化建议

| 优化项 | 预期收益 | 实施难度 |
|--------|----------|----------|
| pgvector IVFFlat/HNSW 索引 | 向量搜索 10-100x 加速 | 低 |
| Next.js Image 组件替换 `<img>` | 图片加载性能提升 | 低 |
| 页面级数据缓存 (`revalidate`) | 减少重复数据库查询 | 低 |
| 留言板活跃筛选下推数据库 | 减少数据传输 | 低 |
| COS 图片处理（缩略图/压缩） | 减少上传带宽和存储 | 中 |
| 大数据量分页 | 防止内存溢出 | 中 |
| API 响应流式传输 (SSE) | 聊天体验更流畅 | 中 |

---

## 四、安全审查

| 风险 | 等级 | 状态 |
|------|------|------|
| 数据库密码硬编码 | 中 | 待修复 |
| 上传文件无类型校验 | 高 | 待修复 |
| cookie 认证可绕过 | 中 | 视部署场景 |
| SQL 注入 | 低 | `$queryRaw` 模板字符串已参数化 |
| XSS (用户生成内容) | 低 | React 默认转义，但需注意 `dangerouslySetInnerHTML` |
| CSRF | 低 | Next.js Server Actions 自带 CSRF 保护 |
| 密钥泄露 | 中 | `.env` 文件不应提交至 Git（需确认 .gitignore） |

---

## 五、总结

本次优化主要解决了 RAG 检索系统的核心瓶颈——中文关键词提取失效问题。该问题是导致"小七同学"无法检索到对应聊天记录回答用户事实性问题的根因。配合停用词精简、RRF 融合、对话历史和查询扩展优化，检索命中率应有显著提升。

功能测试发现了 9 个缺陷（2 个高优、4 个中优、3 个低优），建议按优先级逐步修复。安全方面最需关注的是上传文件类型校验和数据库密码硬编码问题。
